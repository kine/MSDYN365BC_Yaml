# File: InstallModules.yml

steps:
- powershell: |
    Write-Host "*Disabled* All modules are included in the source repository (Needed for DotNet support)"

  failOnStderr: true
  displayName: 'Install Modules'
